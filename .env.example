# Frontend Environment Variables

# API Configuration
# Backend API base URL (with /api suffix)
# Development: Can use proxy (just /api) or full URL
# VITE_API_URL=/api  # Uses Vite dev server proxy to localhost:3001
VITE_API_URL=http://localhost:3001/api  # Direct connection to backend

# Production settings (update for your domain)
# VITE_API_URL=https://api.createkuji.app
